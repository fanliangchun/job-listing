<div id="wrapper">

<h3>提交履历到 <%= @job.title %> </h3>

<hr />
<%= simple_form_for [@job, @resume] do |f| %>
  <%= f.input :content %>
  <%= f.input :attachment %>
  <%= f.submit "送出", :class => "btn btn-success"%>
  <%= link_to("取消", jobs_path, :class => "btn btn-danger")%>
<% end %>

</div>
